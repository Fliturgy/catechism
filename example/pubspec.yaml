name: example
description: An example catechism app.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.0.6

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  catechism:
    path: ../
  flutter_riverpod: ^2.3.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/graphics/